{
  "name": "metaconcurseiro-app",
  "version": "1.0.0",
  "main": "server/index.js",
  "scripts": {
    "build": "cd client && npm install && npx vite build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
