#!/bin/bash
# Instala dependências do frontend
cd client && npm install && cd ..

# Instala dependências do backend
cd server && npm install && cd ..

# Se houver outras ferramentas, adicione seus comandos aqui
